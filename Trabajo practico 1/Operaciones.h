/**
Se realizaron tantas funciones para poder tener un resultado certero de todas las posibles combinaciones entre los datos
*/
//La funcion realiza la suma de dos numeros enteros;
int CalculateSumTypeII(int, int);

//Las funciones siguientes realiza la suma de dos numeros dando de resultado un numero flotante;
float CalculateSumTypeIF(int, float);
float CalculateSumTypeFF(float, float);
float CalculateSumTypeFI(float, int);

//La funcion realiza la resta de dos numeros enteros;
int CalculateSubtractionTypeII(int, int);

//Las funciones siguientes realiza la resta de dos numeros dando de resultado un numero flotante;
float CalculateSubtractionTypeFF(float, float);
float CalculateSubtractionTypeIF(int, float);
float CalculateSubtractionTypeFI(float, int);

//Las funciones siguientes realiza la division de dos numeros dando de resultado un numero flotante;
float CalculateDivisionTypeII(int, int);
float CalculateDivisionTypeFF(float, float);
float CalculateDivisionTypeIF(int, float);
float CalculateDivisionTypeFI(float, int);

//La funcion realiza la multiplicacion de dos numeros enteros;
int CalculateMultiplicationTypeII(int, int);

//Las funciones siguientes realiza la multiplicacion de dos numeros dando de resultado un numero flotante;
float CalculateMultiplicationTypeFF(float, float);
float CalculateMultiplicationTypeIF(int, float);
float CalculateMultiplicationTypeFI(float, int);

//esta funcion calcula el factorial
long int CalculateFactorial(int);
